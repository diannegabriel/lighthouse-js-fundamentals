/*
function findAverage(x, y) {
  var answer = (x + y) / 2;
  return answer;
}

var avg = findAverage(5, 9);

*/

let classNames = ["Clara", "James", "Sara"];

let names = [];

for (let i = 0; i <= classNames.length; i++) {
	names.push(classNames[i]);
}

console.log(names);